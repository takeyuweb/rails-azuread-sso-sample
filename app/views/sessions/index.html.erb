<h1>Sessions#index</h1>
<p>Find me in app/views/sessions/index.html.erb</p>

<% if session[:username].present? %>
  Hello <%= session[:username] %>! <%= link_to 'Sign out', session_path, method: 'delete' %>
<% else %>
  <%= link_to 'Sign in with AzureAD', '/auth/azure_ad' %>
<% end %>
